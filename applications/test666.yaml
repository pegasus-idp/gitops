apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test666
  namespace: argocd
spec:
  project: idp #every application belongs to a single project
  source:
    repoURL: https://github.com/pegasus-idp/gitops
    targetRevision: HEAD
    path: deployed_apps/test666/overlays
  destination:
    server: https://kubernetes.default.svc
    namespace: test666
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
