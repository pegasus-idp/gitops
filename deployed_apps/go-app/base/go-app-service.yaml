---
apiVersion: v1
kind: Service
metadata:
  name: go-app-service
  namespace: go-app
  annotations:
    external-dns.alpha.kubernetes.io/hostname: go-app.pauloponciano.digital
  labels:
    app: go-app
    version: 0.0.1
    backstage.io/kubernetes-id: go-app
spec:
  type: ClusterIP
  ports:
  - name: tcp-node
    port: 8080
  selector:
    app: go-app